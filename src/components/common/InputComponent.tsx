import { Input } from "../ui/input"

type Props = {}

export default function InputComponent({ }: Props) {
    return (
        <div>
            <Input
                type="search"
            />
        </div>
    )
}